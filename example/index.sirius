use std.prelude;
use std.array.vector;

fn main(): Int = {
  print("Hello world");

  let array: Vector[String] = Vector::new(10);
  let i: Int = 0;

  while i < 3 {
    putstr("Enter a string: ");
    let input: String = IO::input();
    i = i + 1;
  }
  
  print(array);
  0
}